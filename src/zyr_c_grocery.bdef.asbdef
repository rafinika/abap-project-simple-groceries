projection implementation in class ZYR_BP_C_GROCERY unique;
strict ( 2 );
use draft;
define behavior for ZYR_C_GROCERY alias ZyrCGrocery
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action checkExpirationDate;
}