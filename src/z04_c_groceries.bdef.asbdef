projection implementation in class ZBP_C_04_GROCERIES unique;
strict ( 2 );
use draft;
define behavior for Z04_C_GROCERIES alias GROCERIES
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action checkExpirationDate;
}